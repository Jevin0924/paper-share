stages:
  - deploy

pages:
  stage: deploy
  image: alpine:3.20
  rules:
    - if: '$CI_COMMIT_BRANCH == "paper-reports"'
    - when: never
  script:
    - rm -rf public
    - mkdir -p public
    - cp -a reports/. public/
  artifacts:
    paths:
      - public
